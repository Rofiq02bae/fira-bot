# API
fastapi
uvicorn[standard]
python-multipart
pydantic
requests
python-dotenv
psutil

# Data + preprocessing
pandas
scikit-learn
nltk
numpy==1.26.4

tensorflow-cpu==2.21.0
# LSTM inference — gunakan tensorflow-cpu di container
# tensorflow di-install terpisah di Dockerfile (bukan dari sini)

# BERT inference
# torch, transformers, dll di-install terpisah di Dockerfile