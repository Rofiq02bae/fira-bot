# Default dependencies for this branch (run API on host + bot in compose).
#
# Notes:
# - Torch is intentionally NOT listed here to avoid accidentally pulling CUDA/NVIDIA wheels.
#   Install CPU-only torch explicitly:
#     python -m pip install --index-url https://download.pytorch.org/whl/cpu "torch==2.5.1+cpu"
# - Training/notebook deps are in requirements.train.txt

-r requirements.api.txt
-r requirements.bot.txt